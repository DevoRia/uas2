функція фібоначчі(n: число): число {
  якщо (n < 2) {
    повернути n
  }
  повернути фібоначчі(n - 1) + фібоначчі(n - 2)
}

друк("Starting UaScript benchmark (Harder)...")
друк("Calculating fib(30)...")

нехай n: число = 30
нехай результат: число = фібоначчі(n)

друк("Result: " + результат)
